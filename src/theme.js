// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3d798d ", // Replace with your desired primary color
    },
    secondary: {
      main: "#3d798d", // Replace with your desired secondary color
    },
  },
});

export default theme;
